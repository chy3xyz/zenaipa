# Zig build artifacts
.zig-cache/
.zig-cache-global/
.zig-global-cache/
zig-out/
zig-pkg/

# Frontend
web/node_modules/
web/dist/

# Frontend env (use web/.env.example as template)
web/.env

# macOS
.DS_Store

# IDE / local state
.cursor/
.remember/

# Reference materials (read-only inputs, not part of this project)
_ref/

# Local database (dev data)
zenaipa.db
*.db

# Uploaded files (runtime data)
uploads/

# Stray root package-lock (real lockfile lives in web/)
package-lock.json

# Local agent sandbox config (machine-specific writable roots)
reasonix.toml
